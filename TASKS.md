1. Langchain API in main.ipynb or main_colab.ipynb
Single prompt response
Single prompt response (batch)
Chatting with the API (with no memory)
Chatting with the API (with memory)


2. Quiz on last session's contents:

How are LLMs different from machine learning models and models that came before them?
What is meant by the statement "LLMs are stateless" and what does it imply for how we send items to LLMs?
What is the context of an LLM 
What is an embedding, and what are the advantages of using embeddings? 
Are fine-tuning and reinforcement learning parts of LLM pre-training? 
What is the difference between RAG and agentic RAG? 
What is a prompt injection, and 
what is the difference between a prompt injection and a jailbreak?
What are some simple ways of avoiding hallucinations? 


3. Bash practice task
These instructions will help you to practise your Bash skills. Replace X with your name.
Display your current folder, then list all its files using the detailed view.
Create a folder called bash_practice and change into it.
Open the current folder in VS Code using code.
Create a folder called tmp.
Use touch to create tmp/hello.txt, then use echo and > to add "Hello, my name is X".
Display the contents of tmp/hello.txt.
Copy it to a new file called tmp/hello2.txt.
Append "and I am an actuary" to tmp/hello2.txt using echo and >>.
Use grep -n to search for "actuary" in tmp/hello2.txt.
Use nano to create notes.txt, add a short message, save it and exit.
Rename tmp/hello2.txt to tmp/profile.txt; enter tmp, then use cd - to return.
Check your location, delete hello.txt and profile.txt using rm, then list tmp to confirm.